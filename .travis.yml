language: node_js

node_js:
 - "11.8.0"

 services:
  - postgresql
  
  addons:
  postgresql: "10"
  apt:
    packages:
    - postgresql-10
    - postgresql-client-10
env:
  global:
  - PGPORT=5433

cache:
directories:
  - "node_modules"

script:
  - npm run test

after_script:
  - npm run coverage
